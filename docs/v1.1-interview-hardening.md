# v1.1 面试问题驱动验证说明

## 已实现

- `GET /trace/llm/{request_id}`：返回一次请求的 LLM 调用明细、输入/输出/总 Token、延迟和可选成本。
- `GET /trace/llm`：查看最近的 LLM 观测记录。
- `/chat` 响应中的 `llm_usage`：便于直接展示单请求证据。
- `/metrics` 中的 `coordexa_llm_*`：Prometheus 采集调用数、Token、缺失 usage、延迟和活跃调用。
- `GET /trace/llm/overview`：Swagger/前端一键查看总 Token、usage 可用率、组件聚合和请求聚合；`GET /trace/llm/{request_id}` 保留单请求明细。
- provider 没有返回 `usage` 时只标记 `usage_available=false`，不根据字符数伪造 Token。
- Agent 空文本自动重试一次；多 Agent 结果包含 `degraded` 和 `degraded_agents`。
- 上下文按提示、记忆、检索结果分区预算；Chunk 支持大小、overlap 和超长句硬切配置。

## 参数

价格不写入代码。只有同时设置以下两个环境变量才会产生 `estimated_cost_usd`：

```text
COORDEXA_INPUT_COST_PER_1M=
COORDEXA_OUTPUT_COST_PER_1M=
```

上下文与切片实验参数见 `.env.example`。默认 Chunk 设置仍为 500 字、0 overlap，以便与 v1.0 基线可比。

## 评测命令

确定性测试在容器内运行：

```bash
docker compose --profile test run --rm tests
```

当前分支基线为 70 条测试。在线稳定性扩展集：

```bash
python scripts/run_benchmark.py \
  --base-url http://localhost:8200 \
  --cases evaluation/fixtures/benchmark_cases_v2.json \
  --top-k 3 \
  --output evaluation/results/coordexa_v2_benchmark.json
```

v2 由 12 条对话和 6 条检索标注样本重复 4 轮组成，实际完成 48 条对话请求和 24 条检索请求。v1.1 基线请求成功率均为 100%；意图准确率 83.33%，主 Agent 路由、Agent 覆盖、RAG 门控和实体用例均为 100%，检索 Hit@3/MRR 为 100%/1.0000。重复执行用于观察真实 API 链路稳定性，不宣称生产准确率。完整基线报告见 `evaluation/results/coordexa_v2_benchmark_2026-09-20.json`。

## v2.0 意图误判修复复测

针对基线中稳定出现的“订单什么时候到”→`query` 和“账号疑似被盗”→`refund`，增加细粒度模板、上下文组合关键词和安全类显式规则优先级；同时补充回归测试，避免“退款多久到账”被宽泛的“多久到”误判为物流。最终同一 v2 扩展集复测结果为：48/48 对话、24/24 检索成功；意图、主 Agent、Agent 覆盖、RAG 门控和实体用例均 100%；Hit@3=100%、MRR=1.0000。最终平均/P50/P95 端到端延迟为 26.62/26.33/54.39 秒。详细报告见 `evaluation/results/coordexa_v2_benchmark_2026-09-20-intent-fix-final.json`。

## 真实烟测记录

2026-09-20，在运行副本的 DeepSeek Anthropic-compatible API 上完成 `/chat` 烟测：

- 请求 ID：`c5d88000`
- 组件调用：`intent.llm`、`agent.general`
- LLM 调用数：2
- 输入 Token：1858
- 输出 Token：216
- 总 Token：2074
- usage 可用调用：2/2

价格环境变量为空，因此成本字段保持 `null`，这属于可解释的“未配置价格”，不是估算值。
